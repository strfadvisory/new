version: '3.8'
services:
  client:
    build: ./client
    ports:
      - "5000:5000"
    volumes:
      - ./client:/app
      - /app/node_modules
    environment:
      - REACT_APP_API_URL=https://208.109.39.222:5001/api
      - PORT=5000

  server:
    build: ./server
    ports:
      - "5001:5001"
    volumes:
      - ./server:/app
      - /app/node_modules
    environment:
      - MONGO_URI=mongodb+srv://anupamurl:Espl@123@strf.liiptbb.mongodb.net/simulator-dev
      - PORT=5001

  mongo:
    image: mongo:latest
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
